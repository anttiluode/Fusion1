[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "fusion1"
version = "0.1.0"
description = "A control plane for persistent AI computation"
requires-python = ">=3.10"
license = {text = "MIT"}
authors = [{name = "Antti Luode"}]

[tool.setuptools]
packages = ["fusion1"]
